[require]
GLSL >= 1.10

[vertex shader passthrough]

[fragment shader]
vec4 color;
void main()
{
   color = vec4(0.0, 0.0, 0.0, 0.0);
   while (color.y < 0.5)
   {
      color.y = color.y + 1.0;
   }
   gl_FragColor = color;
}

[test]
draw rect -1 -1 2 2
probe rgba 1 1 0.0 1.0 0.0 0.0
